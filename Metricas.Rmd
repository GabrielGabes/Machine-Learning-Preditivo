```{r}
library(caret)
tabela = rbind(c(183,141),c(13,663))

confusionMatrix(tabela)

confusionMatrix(tabela, mode = "prec_recall")
```


```{r}
library(gmodels)
CrossTable(tabela)
```


```{r}
```


```{r}
```


```{r}
```

